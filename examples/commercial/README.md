# Example: Commercial AWS

Deploys the root module in a commercial region (default `us-west-2`).

```bash
terraform init
terraform apply
```

Use a commercial AWS account and credentials. GovCloud credentials will not work here.
